CHIP Fanout {
  IN
    in;

  OUT
    out[16];

  PARTS:
    And16(
      out=out,
      a[0]=in,a[1]=in,a[2]=in,a[3]=in,a[4]=in,a[5]=in,a[6]=in,a[7]=in,a[8]=in,a[9]=in,a[10]=in,a[11]=in,a[12]=in,a[13]=in,a[14]=in,a[15]=in,
      b[0]=true,b[1]=true,b[2]=true,b[3]=true,b[4]=true,b[5]=true,b[6]=true,b[7]=true,b[8]=true,b[9]=true,b[10]=true,b[11]=true,b[12]=true,b[13]=true,b[14]=true,b[15]=true
    );
}
